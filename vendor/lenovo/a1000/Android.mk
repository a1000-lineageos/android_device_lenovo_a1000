# Android.mk — vendor/lenovo/a1000
# Большинство блобов кладётся через PRODUCT_COPY_FILES (a1000-vendor.mk).
# Файл оставлен для будущих prebuilt-модулей (если понадобится PRODUCT_PACKAGES).
LOCAL_PATH := $(call my-dir)
ifeq ($(TARGET_DEVICE),a1000)
endif
